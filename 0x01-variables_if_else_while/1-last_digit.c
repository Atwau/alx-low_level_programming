#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/**
 * main - program to print last digit
 * Return: 0 for success
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	/* your code goes there */
	return (0);
}
