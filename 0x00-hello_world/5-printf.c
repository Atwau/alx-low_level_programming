#include <stdio.h>

/**
 * main - this is the entry point to the program
 * Return: this outputs 0 when the function is executed sucessfully
 */

int main(void)
{

	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
